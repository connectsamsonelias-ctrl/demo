# db/schema

Reserved for generated/reference schema documentation once the full data
model lands in Milestone 2. The actual schema is defined by the
migrations in `db/migrations/`, which are the source of truth.
