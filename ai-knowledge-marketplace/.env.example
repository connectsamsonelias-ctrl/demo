# Copy to .env.local for local development. Never commit real secrets.

# --- Database (Milestone 1: connection layer only) ---
DATABASE_URL=postgres://user:password@localhost:5432/ai_knowledge_marketplace

# --- Auth (provider not yet selected — see docs/decisions/0001-auth-provider.md) ---
# AUTH_PROVIDER=dev-stub

# --- Object storage (selected in a later milestone) ---
# OBJECT_STORAGE_BUCKET=
# OBJECT_STORAGE_REGION=
# OBJECT_STORAGE_ACCESS_KEY_ID=
# OBJECT_STORAGE_SECRET_ACCESS_KEY=

# --- Payments (provider not yet selected — see kickoff review, Step 2A) ---
# PAYMENTS_PROVIDER=
# PAYMENTS_SECRET_KEY=
# PAYMENTS_WEBHOOK_SECRET=

# --- AI (external model API, vendor not yet pinned) ---
# AI_PROVIDER_API_KEY=

NODE_ENV=development
