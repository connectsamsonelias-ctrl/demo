# workers

- `audit/` — the Knowledge Audit worker (Milestone 7). Implemented: see
  `audit/processor.ts` and `npm run worker:audit`.
- `content/`, `notifications/` — still reserved for later milestones
  (further content-processing pipeline stages, and notification
  delivery). Not implemented yet.
